import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export default function SaveResultsPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Save results</CardTitle>
      </CardHeader>
      <CardContent className="text-sm text-muted-foreground">
        Placeholder route shell. Implement the opt-in + client upsert per Epic 03.
      </CardContent>
    </Card>
  );
}

